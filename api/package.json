{
  "name": "voltpass-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "VoltPass server-side Smartcar connector. Holds the Smartcar Client Secret and access tokens, calls the Smartcar API, and writes results into SpacetimeDB through persist-only reducers. Never exposes secrets to the frontend.",
  "scripts": {
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "smartcar": "^10.4.0",
    "spacetimedb": "1.12.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.5.0",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4"
  }
}
